function Loading() {
  return (
    <main
      style={{
        padding: '24px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '100vh',
      }}
    >
      <h1 style={{ textAlign: 'center' }}>Loading...</h1>
    </main>
  );
}

export default Loading;
